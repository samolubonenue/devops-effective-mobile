# Effective Mobile - Docker Environment Variables
# Copy this file to .env and adjust values as needed

# Nginx external port (exposed to host)
NGINX_PORT=80

# Backend internal port
BACKEND_PORT=8080
